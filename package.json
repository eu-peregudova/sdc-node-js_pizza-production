{
  "name": "pizza-production",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start:all": "npm run start --workspace=apps/shipment-service & npm run start --workspace=apps/ordering-service & npm run start --workspace=apps/production-service & wait",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint src --ext .ts --fix && npm run format",
    "format": "prettier --write \"apps/**/src/**/*.ts\""
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
